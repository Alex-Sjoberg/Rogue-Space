

def parseMovies():
    file = open("Movies.txt" , 'r')
    outFile = open("Parsed.txt", 'w')

    accum = 0
    
    for line in file:
        if "<tr" in line:
            accum += 1
            titleLine = file.readLine()
            namestart = titleLine.index("title")
            titleLine = titleLine [nameStart:]

            
            
